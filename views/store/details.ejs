<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Details of <%= item.name %></title>
  <link rel="stylesheet" href="/src/output.css"/>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <!-- Navbar -->
  <%- include("../partials/navBar") %>

  <!-- Product Details Section -->
  <section class="max-w-5xl mx-auto px-6 py-12 flex-1">
    <div class="bg-white rounded-xl shadow overflow-hidden flex flex-col md:flex-row">
      
      <!-- Product image -->
      <div class="md:w-1/2">
        <img src="/images/<%= item.image %>" alt="<%= item.name %>" 
             class="w-full h-64 md:h-full object-cover">
      </div>

      <!-- Product info -->
      <div class="p-6 md:w-1/2 flex flex-col justify-center">
        <h1 class="text-2xl md:text-3xl font-bold text-gray-800"><%= item.name %></h1>
        <p class="text-gray-600 mt-2">Category: <%= item.category %></p>
        <p class="text-indigo-700 font-bold mt-4 text-xl">₹<%= item.price %></p>

        <!-- Action buttons -->
        <div class="mt-6 flex gap-4">
          <a href="/store" 
             class="bg-gray-200 text-gray-800 px-4 py-2 rounded-lg hover:bg-gray-300 transition">
            Back to Store
          </a>
          
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gradient-to-r from-indigo-700 via-blue-700 to-indigo-600 text-white text-center py-4">
    <p>&copy; <%= new Date().getFullYear() %> MyApp Store</p>
  </footer>

</body>
</html>